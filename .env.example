# Authentication Configuration
# ------------------------------------------------------------------------------
# Secret key used for authentication. Ensure this is a long, secure, and random value.
# Never share this key publicly.
AUTH_SECRET="your_very_long_and_secure_secret_here"

# Optional: Specify the base URL of your application (useful for authentication callbacks).
# Example: http://localhost:3000 for local development.
AUTH_URL="http://localhost:3000"

# Database Configuration
# ------------------------------------------------------------------------------
# Connection string for your database.
# Replace with your database provider (e.g., MySQL, PostgreSQL, etc.) and credentials.
# Example for MySQL:
# DATABASE_URL="mysql://username:password@localhost:3306/database_name"
DATABASE_URL=""
